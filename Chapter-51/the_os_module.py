import os

# create directory recursively using OS Module => create directory inside a directory that does not exist. All the directories will be created using the command ==>
# os.makedirs('Chapter-51/dir1/subdir1/sub-subdir1')

# create a directory
# os.mkdir('Chapter-51/dir2')

# get current directory
# print(os.getcwd())

print(os.name)
